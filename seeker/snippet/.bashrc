#date: 2022-09-22T17:20:23Z
#url: https://api.github.com/gists/856d24187e42cf40333346bf1ac5740f
#owner: https://api.github.com/users/rafaelqueiroz88

# NÃO COPIAR ESSE BLOCO, APENAS USE COMO REFERÊNCIA

# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
# [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

# Esse comando precisa estar ABAIXO da configuração do RVM (o que está comentado nas linhas acima

if test -f ~/.rvm/scripts/rvm; then
  [ "$(type -t rvm)" = "function" ] || source ~/.rvm/scripts/rvm
fi